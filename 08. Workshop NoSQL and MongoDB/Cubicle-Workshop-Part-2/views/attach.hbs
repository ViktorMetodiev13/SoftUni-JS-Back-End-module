<h1>Attach a new accessory</h1>
<div class="form">
  <h2>{{cube.name}}</h2>
  <img class="cube" src="{{cube.imageUrl}}">
  {{#if accessories}}
  <form action="/products/details/{{cube._id}}" method="">
    <label for="accessory">Accessories</label>
    <select id="accessory" name="accessory">
      {{#each accessories}}
      {{> accessory}}
      <option value="{{accessory._id}}">{{accessory.name}}</option>
      {{/each}}
    </select>
    <input type="submit" value="Attach">
  </form>
  {{else}}
  <h3 class="italic">This cube has all available accessories so far or there is no available at all...
  {{/if}}
  </h3>
  <a class="btn" href="/products/details/{{cube._id}}">Back</a>
</div>